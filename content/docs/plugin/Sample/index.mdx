---
title: 插件开发帮助
description: 帮助你快速开始
---

## 插件接口一览

### 1 元数据字段

元数据的**写在哪**取决于插件形态：

| 形态 | `name` / `type` / `version` / `description` |
|------|-----------------------------------------------|
| **包插件**（目录 + `pluginType`） | 复用 `package.json` 的 `name`/`version`/`description`，另加 `pluginType`，框架注入实例 |
| **单文件插件** | 写在类字段上 |

<TypeTable
  type={{
    name: {
      description: '插件唯一名称（运行时 id；禁用与依赖声明用它）',
      required: true,
      type: 'string',
    },
    type: {
      description: '插件适用的运行主体',
      type: <code>"user" | "bot" | "general"</code>,
      default: <code>"general"</code>,
    },
    version: {
      description: '插件版本号',
      required: true,
      type: 'string',
    },
    description: {
      description: '简短描述',
      required: true,
      type: 'string',
    },
    longDescription: {
      description: '可选的长描述',
      type: 'string',
    },
  }}
/>

#### 包插件：元数据在 package.json

```json title="package.json"
{
  "name": "my-plugin",
  "version": "1.0.0",
  "description": "简短描述",
  "main": "index.ts",
  "type": "module",
  "pluginType": "general",
  "dependencies": {
    "@fuyu-tdbot/plugin-api": "workspace:*"
  }
}
```

```ts title="index.ts"
import { Plugin } from "@fuyu-tdbot/plugin-api";
import type { Client } from "tdl";

export default class MyPlugin extends Plugin {
  // 不要在类里再写 name/version/description/pluginType

  constructor(client: Client) {
    super(client);
  }
}
```

#### 单文件插件：元数据在类字段

```ts title="Examples.ts"
import { Plugin } from "@fuyu-tdbot/plugin-api";
import type { Client } from "tdl";

export default class LifePlugin extends Plugin {
  name = "插件唯一名称"; // [!code highlight]
  type = "general"; // [!code highlight]
  version = "插件版本号"; // [!code highlight]
  description = "简短描述"; // [!code highlight]

  constructor(client: Client) {
    super(client);
  }
}
```

`pluginType` 用来指明插件运行的环境

- `user`：插件中包含只能使用 user 账户能使用的功能（如删除 48 小时前消息）。
- `bot`：依赖 Bot API 特性（如内联按钮）。
- `general`：两侧均可使用。

包插件写在 `pluginType`：

```json title="pluginType 字段"
// [!code word:"general"]
"pluginType": "general"
```

从旧类字段迁到 package.json 清单 见[插件迁移指南](/docs/plugin/Migration)。

### 2 可选生命周期 & 运行任务

| 字段/方法     | 作用                                                                                                       |
| ------------- | ---------------------------------------------------------------------------------------------------------- |
| `onLoad()`    | 插件被实例化并注册后立即调用，用于一次性初始化。                                                           |
| `runHandlers` | 注册可直接触发或定时执行的任务。支持 `cron` 表达式或 `intervalMs` 轮询，`immediate` 可在加载时先运行一次。 |
| `destroy()`   | 卸载前调用，用于清理资源、关闭定时器等。                                                                   |

`runHandlers` 的定义：

```ts
type RunDef = {
  description?: string;
  handler: () => Promise<void> | void;
  cron?: string; // 优先使用 cron，依赖 cron 包，支持秒级表达式
  intervalMs?: number; // 毫秒级轮询
  immediate?: boolean; // 加载后立即执行一次
};
```
